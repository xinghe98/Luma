# 本地媒体管理播放器整体产品规划

## 1. 产品暂定名称

```text
项目代号：LocalMedia
中文暂定名：本地影库
```

名称仅用于开发阶段，正式发布前再进行品牌命名。

---

## 2. 产品定位

这是一个部署在个人服务器、家庭服务器或内网环境中的自托管视频和图片管理播放器。

它将普通的文件目录转换成类似视频网站的媒体库体验：

```text
文件夹中的视频和图片
→ 自动扫描和生成封面
→ 网格浏览
→ 搜索和筛选
→ 查看详情
→ 直接播放
→ 保存进度、标签和笔记
```

产品不是传统文件管理器，也不是单纯的 SMB 播放器。

核心定位：

> 用视频网站式的体验，管理和播放存放在个人服务器或内网中的视频与图片。

---

## 3. 目标用户

第一阶段目标用户：

* 家中有 NAS、迷你主机或旧电脑的用户
* 视频和图片数量较多的用户
* 使用 SMB 或本地硬盘保存媒体的用户
* 不希望将私人媒体上传到云端的用户
* 认为传统文件夹浏览体验较差的用户
* 希望在手机上快速搜索和播放内网视频的用户

第一版优先服务单用户，不针对大型团队和企业媒体资产管理。

---

## 4. 核心问题

用户当前通常通过以下方式管理媒体：

```text
文件夹
SMB 文件浏览器
普通视频播放器
系统相册
```

这些方式存在的问题：

* 大量文件难以查找
* 文件名不直观
* 缺少统一缩略图
* 视频和图片分散
* 无法按标签和状态分类
* 缺少播放进度同步
* 手机端 SMB 浏览体验较差
* 很难形成视频网站式的内容浏览体验

---

## 5. 核心价值

### 5.1 数据留在本地

原始媒体文件不上传第三方云服务。

### 5.2 自动媒体化

普通文件自动变成：

```text
封面
标题
时长
分辨率
编码信息
播放入口
```

### 5.3 像视频网站一样浏览

不再通过层层文件夹寻找视频，而是通过：

```text
首页推荐区域
最近添加
继续观看
收藏
搜索
筛选
标签
文件夹
```

### 5.4 多端统一体验

同一个服务端后续可以连接：

```text
手机 App
平板 App
桌面客户端
Web 客户端
电视端
```

---

## 6. 产品形态

整体由两个部分组成：

### 6.1 服务端

部署在：

```text
NAS
Linux 服务器
Windows 主机
迷你电脑
家庭服务器
Docker 环境
```

服务端负责：

* 扫描媒体
* 建立索引
* 生成缩略图
* 提取元数据
* 保存用户数据
* 提供视频流
* 提供 API

服务端技术基线：

```text
Go
Gin 路由和 API 中间件
net/http.Server
http.ServeContent 视频 Range
SQLite
构造函数手动依赖注入
```

正式支持环境：

```text
Linux x64
Docker（Linux）
Windows x64 直接运行和后台服务
```

### 6.2 客户端

第一阶段：

```text
Flutter 手机 App
```

后续：

```text
平板适配
Windows 桌面端
Web 客户端
Android TV
```

---

## 7. 产品核心原则

### 原则一：原文件优先

服务端默认不修改、不移动、不重命名、不删除原文件。

### 原则二：Direct Play 优先

第一阶段直接播放原视频文件，不立即开发实时转码。

### 原则三：本地网络优先

第一阶段只服务局域网，不处理公网穿透。

局域网不等于可信网络。除健康检查外，服务端从技术验证版开始即使用最低限度的 API Token，并限制可扫描的根目录。

### 原则四：体验优先于功能数量

先把以下体验做好：

```text
打开 App
→ 看到漂亮媒体网格
→ 快速找到内容
→ 点击立即播放
```

### 原则五：客户端与存储解耦

客户端不直接连接 SMB，不直接理解服务器文件系统。

### 原则六：逐步扩展

第一版先解决浏览和播放，后续再增加刮削、转码和多用户。

### 原则七：跨平台可验证

服务端的 Linux 与 Windows 支持必须通过真实平台测试验证，不能仅依赖路径字符串模拟。平台差异集中隔离，不进入业务逻辑。

---

## 8. 信息架构

```text
App
├── 首次设置
│   ├── 添加服务器
│   ├── 输入访问令牌
│   ├── 测试连接
│   └── 连接成功
│
├── 首页
│   ├── 继续观看
│   ├── 最近添加
│   ├── 收藏
│   └── 最近播放
│
├── 媒体库
│   ├── 全部
│   ├── 视频
│   ├── 图片
│   ├── 文件夹
│   └── 标签
│
├── 搜索
│   ├── 关键词搜索
│   ├── 类型筛选
│   ├── 标签筛选
│   ├── 时长筛选
│   └── 排序
│
├── 媒体详情
│   ├── 封面
│   ├── 播放
│   ├── 收藏
│   ├── 标签
│   ├── 笔记
│   ├── 文件信息
│   └── 媒体信息
│
├── 播放器
│   ├── 播放与暂停
│   ├── 进度拖动
│   ├── 快进与后退
│   ├── 全屏
│   └── 进度保存
│
└── 设置
    ├── 服务器管理
    ├── 媒体源
    ├── 扫描状态
    ├── 缓存管理
    └── 关于
```

---

## 9. 核心用户流程

### 9.1 第一次使用

```text
安装 App
→ 输入服务端地址
→ 输入或扫码导入 API Token
→ 测试连接
→ 服务端没有媒体源
→ 引导用户在服务端配置目录
→ 触发扫描
→ 等待首批媒体出现
→ 进入媒体库
```

### 9.2 日常浏览

```text
打开 App
→ 首页展示最近添加和继续观看
→ 点击媒体卡片
→ 进入详情或直接播放
```

### 9.3 搜索媒体

```text
进入搜索
→ 输入关键词
→ 查看结果
→ 使用标签或类型筛选
→ 进入详情
```

### 9.4 继续观看

```text
播放视频
→ 中途退出
→ 服务端保存进度
→ 再次打开 App
→ 首页继续观看
→ 从原位置播放
```

### 9.5 添加新媒体

```text
用户将文件复制到服务器目录
→ 服务端扫描
→ 提取元数据
→ 生成缩略图
→ App 最近添加区域出现新内容
```

---

## 10. 产品版本规划

### V0.1：技术验证版

目标：

> 证明“服务端扫描 + App 网格 + Range 播放”能够稳定工作。

功能：

服务端：

* 固定媒体目录
* 固定目录白名单
* API Token
* Gin 路由和中间件
* 构造函数手动依赖注入
* 扫描视频
* ffprobe
* 单张缩略图
* 媒体列表 API
* Stream API
* HTTP Range
* Linux 和 Windows 控制台启动

App：

* 固定服务器地址
* 固定或手动输入 API Token
* 视频网格
* 媒体详情
* 视频播放
* 进度拖动

不做：

* 图片
* 搜索
* 标签
* 笔记
* 收藏
* 播放进度
* 设置页面

完成标准：

```text
一个目录中放入 10 个不同视频
App 能展示封面
全部能够尝试播放
播放器能够暂停和拖动
同一套 API 在 Linux 和 Windows 上通过健康检查和播放闭环验证
```

---

### V0.2：最小可用版本

目标：

> 产品可以被自己日常使用。

服务端增加：

* SQLite
* 多媒体源
* 视频和图片
* 增量扫描
* 媒体详情
* 收藏
* 标签
* 笔记
* 播放进度
* 扫描状态
* SQLite 持久化任务和重启恢复
* 文件改名和移动后保留媒体 ID
* 默认本地用户及可扩展的 user_id 数据结构
* API Token 配置
* 媒体目录白名单
* Windows 盘符和 UNC 媒体源
* Windows PowerShell 开发与构建脚本

App 增加：

* 服务器配置
* 首页
* 媒体库
* 搜索
* 筛选
* 收藏
* 标签
* 笔记
* 继续观看
* 扫描状态

完成标准：

```text
可以管理一个真实媒体目录
可以连续使用一周
重新扫描不会丢失用户数据
服务器和 App 重启后状态正常
扫描失败或存储临时离线不会误标记大量媒体缺失
未认证设备无法访问媒体库
Windows 路径大小写变化不会重复索引媒体
```

---

### V0.3：体验优化版

目标：

> 从“能使用”提升到“愿意使用”。

功能：

* 深色主题完善
* 平板布局
* 网格列数调整
* 缩略图缓存
* 下拉刷新
* 骨架屏
* 搜索历史
* 播放器手势
* 播放速度
* 屏幕方向控制
* 自定义封面
* 文件夹浏览
* 扫描失败重试
* 扫描任务中断恢复提示
* 服务端日志查看
* 缩略图重新生成

完成标准：

```text
常见错误有明确提示
大量媒体滚动流畅
服务器暂时离线不会造成数据混乱
播放器体验达到日常使用标准
```

---

### V0.4：媒体库增强版

目标：

> 解决大规模媒体的整理和发现问题。

功能：

* 组合筛选
* 自定义媒体标题
* 多级标签
* 评分
* 已观看状态
* 批量编辑标签
* 批量收藏
* 重复文件初步识别
* 多张候选封面
* 视频雪碧图预览
* 图片 EXIF 信息
* 图片瀑布流
* 文件变化自动监听
* 定时扫描

完成标准：

```text
数千个媒体文件仍然能够高效查找
用户可以不依赖原始目录结构完成大部分浏览
```

---

### V0.5：存储扩展版

目标：

> 支持更常见的家庭存储环境。

功能：

* SMB 挂载目录支持
* App 内媒体源管理
* 网络存储状态检测
* 媒体源离线状态
* 多个媒体源聚合
* 媒体源单独扫描
* 缩略图缓存清理策略

优先方案：

```text
宿主机挂载 SMB
→ 服务端将挂载目录作为 LocalSource
```

宿主机挂载的 SMB 不单独实现新的存储驱动，而是作为 `LocalSource` 使用并增加在线状态检测。后期再考虑服务端原生 SMB 客户端；原生 SMB 的连接信息使用版本化配置，凭据通过独立安全存储引用，不明文写入普通配置或数据库 JSON。

---

### V1.0：正式可发布版本

目标：

> 面向普通自托管用户发布。

必须具备：

* Docker 一键部署
* 完整安装文档
* 配置向导
* 数据库迁移
* 数据备份和恢复
* 健康检查
* 日志导出
* 媒体源管理
* 扫描任务管理
* 稳定的视频播放
* 标签、收藏、笔记和进度
* Android App
* 基础平板适配
* 完整异常状态
* API Token 管理、轮换和重新配对
* 路径白名单和安全审计
* API 版本兼容策略
* Windows x64 发布包
* Windows 后台服务安装和卸载文档
* Linux 与 Windows CI 测试矩阵

正式版仍可不包含：

* 实时转码
* 公网访问
* 在线影视刮削
* AI 识别
* 多用户
* TV 客户端

---

## 11. 后续版本方向

### V1.x

* iOS
* Windows 客户端
* Web 客户端
* 字幕识别和选择
* 多音轨选择
* 视频倍速
* 批量操作
* 文件夹封面
* 媒体合集
* 服务端自动更新提示

### V2.0

* 多用户
* 用户权限
* 家庭共享
* HLS
* 按需转码
* 预转码
* 外网安全访问
* Android TV
* 影视信息刮削
* 演员和系列管理

---

## 12. 开发里程碑

### 里程碑 A：播放闭环

```text
服务端扫描一个目录
→ App 展示视频封面
→ 点击播放
→ 支持拖动
```

这是最高优先级。

### 里程碑 B：媒体管理闭环

```text
搜索
→ 查看详情
→ 收藏
→ 标签
→ 笔记
```

### 里程碑 C：日常使用闭环

```text
播放进度
→ 继续观看
→ 新文件扫描
→ 最近添加
```

### 里程碑 D：稳定部署闭环

```text
Docker 部署
→ 数据持久化
→ 任务中断恢复
→ Token 认证
→ Windows 后台服务
→ 自动重启
→ 备份恢复
```

---

## 13. 开发优先级

### P0：没有就不能使用

* 扫描目录
* 媒体列表
* 缩略图
* 视频流
* HTTP Range
* API Token
* 媒体目录白名单
* Gin 路由和统一中间件
* Linux 与 Windows 服务端启动
* App 服务器连接
* 媒体网格
* 视频播放器

### P1：决定是否愿意日常使用

* 搜索
* 筛选
* 详情
* 收藏
* 播放进度
* 继续观看
* 扫描状态
* 扫描失败保护和任务恢复
* 错误提示

### P2：提升整理体验

* 标签
* 笔记
* 文件夹
* 自定义标题
* 自定义封面
* 批量操作

### P3：扩展性功能

* SMB
* 多用户
* 转码
* 刮削
* TV
* 公网访问
* AI 分类

开发时必须严格按照 P0 → P1 → P2 → P3 推进。

---

## 14. 产品成功标准

第一阶段不以用户数量衡量，而以自身使用体验衡量。

核心指标：

```text
扫描成功率
改名或移动后的媒体身份保留率
缩略图生成成功率
视频直接播放成功率
播放进度恢复成功率
首页加载时间
搜索响应时间
App 崩溃次数
服务端异常退出次数
任务中断恢复成功率
未授权访问拦截率
Linux 和 Windows 构建成功率
```

关键体验标准：

* 打开 App 后快速看到已有媒体
* 常用视频在几步内可以找到
* 点击视频后可以顺利开始播放
* 播放器拖动进度可靠
* 服务器短暂离线后 App 能恢复
* 新增文件可以自动或手动进入媒体库
* 重新扫描不会破坏标签和播放进度
* 文件改名或移动后尽量保留收藏、标签和进度
* 扫描中断或媒体源离线不会把整个媒体库误标记为缺失
* 未持有 Token 的设备无法读取媒体信息和原始内容

---

## 15. 当前最合适的执行顺序

```text
1. 画低保真原型
2. 确定媒体列表、详情字段和 OpenAPI 契约
3. 确定稳定媒体 ID、扫描事务和任务恢复规则
4. 建立 Go、Gin、Composition Root 与 SQLite 迁移
5. 建立 Linux 和 Windows 开发、构建与 CI 基线
6. 实现 API Token、目录白名单和健康检查
7. 实现扫描、缩略图和 Stream API
8. 在 Linux 和 Windows 跑通服务端播放闭环
9. 建立 Flutter 项目
10. 实现服务器和 Token 连接
11. 实现真实媒体网格
12. 实现播放器
13. 跑通端到端播放闭环
14. 增加增量扫描、搜索、收藏和播放进度
15. 再进行高保真 UI 优化
```

不要按照下面的方式开发：

```text
先完成全部后端
→ 再完成全部前端
```

推荐采用纵向切片：

```text
媒体列表后端
→ 媒体列表前端

媒体详情后端
→ 媒体详情前端

视频流后端
→ 播放器前端

播放进度后端
→ 继续观看前端
```

每完成一个功能，都能在真实 App 中立即验证。
